{
  "private": true,
  "workspaces": ["compositor-engine", "compositor-template"],
  "scripts": {
    "start": "yarn workspace compositor-engine start",
    "test": "yarn workspace compositor-engine test"
  }
}
